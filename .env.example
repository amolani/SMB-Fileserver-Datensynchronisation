# Host paths only. Container paths are defined in docker-compose.yml and config.properties.
CONFIG_FILE=./config.properties
AUDIT_LOG_DIRECTORY=/var/log/fullauditlog
SOURCE_DIRECTORY=/srv/samba/schools/default-school
SSH_KEY_FILE=./secrets/id_ed25519
SSH_KNOWN_HOSTS_FILE=./secrets/known_hosts
